# 推箱子（Sokoban）MVP 开发计划

> 本文件是项目唯一的开发依据。后续每一步开发都严格按此执行，每完成一步就停下来 review / commit。
> 文档中的 API 均已对照本地 `node_modules/phaser`（**4.2.1**）类型定义核实过，优先以本文件为准，不要照搬 Phaser 3 的网上教程。

---

## 1. 项目概述

- **目标**：做一个能玩的最小推箱子 demo（MVP）。
  - 有一个关卡，能移动玩家、推箱子、判定通关。
  - 美术用纯色块（Phaser 自带的 Shape 图形对象），不加载图片，先把逻辑跑通。
- **技术栈**：React 19 + Vite + **Phaser 4.2.1** + TypeScript。
- **分工原则**：React 只负责「宿主」——渲染一个容器 div 并挂载 Phaser canvas；游戏逻辑全部在 Phaser 里，不依赖 React 状态。
- **当前状态**：Vite 脚手架已初始化完毕（`package.json` 里已有 `phaser@^4.2.1`）。

---

## 2. 目录结构（目标形态）

```
src/
  game/
    config.ts          // Phaser.Game 配置 + 场景注册
    constants.ts       // 网格尺寸、颜色、图块尺寸等常量
    types.ts           // Cell 枚举、关卡类型等共享类型
    data/
      levels.ts        // 关卡数据（纯数据，二维字符数组）
    scenes/
      BootScene.ts     // 资源加载场景（MVP 阶段可简化）
      GameScene.ts     // 主游戏场景：渲染、输入、推箱子逻辑、胜负判定
    utils/
      level.ts         // 关卡字符串 → 运行时结构的解析函数
  App.tsx              // React 入口：useRef + useEffect 挂载 Phaser
  main.tsx             // Vite 入口（脚手架自带，一般不动）
```

> MVP 阶段 `BootScene` 可以只做「空场景」或干脆省略。是否保留见步骤 2。

---

## 3. 核心约定（贯穿所有步骤，务必遵守）

### 3.1 数据驱动，静态/动态分离

这是推箱子的灵魂，也是最容易写错的点。**运行时把数据分成三层**：

1. **静态网格 `grid`**：只存「墙 / 地板 / 目标点」。玩家和箱子**不写进** `grid`。
2. **箱子列表 `boxes: {x, y}[]`**：动态。
3. **玩家位置 `player: {x, y}`**：动态。

这样「玩家踩过目标点、把箱子推离目标点」这类情况，永远不会污染地图数据——目标点始终留在 `grid` 里。

### 3.2 关卡定义格式（输入格式，标准 Sokoban 记法）

关卡用二维字符串数组书写，每行长度必须一致。字符含义：

| 字符 | 含义 |
|------|------|
| `#` | 墙（Wall） |
| ` `（空格）| 地板（Floor） |
| `.` | 目标点（Goal，一个地板格，箱子要推到这） |
| `$` | 箱子（Box，位于地板） |
| `*` | 箱子压在目标点上（Box on Goal） |
| `@` | 玩家（Player，位于地板） |
| `+` | 玩家站在目标点上（Player on Goal） |

示例：

```
########
#      #
# .$@  #
#   .$ #
#      #
########
```

解析后得到：
- `grid`：`#`→`Cell.Wall`；` `、`.`、`$`、`*`、`@`、`+` 的格子→`Cell.Floor`，其中 `.`/`*`/`+` 额外标为 `Cell.Goal`。
- `boxes`：来自 `$` 和 `*`。
- `player`：来自 `@` 和 `+`。

> 这套记法网上有海量现成关卡（搜 "sokoban levels"），后续加关卡就是加一段字符串数组，扩展性最好。

### 3.3 网格坐标与像素坐标

- 网格坐标 `(col, row)`，用整数，从 0 开始。
- 像素坐标 = 网格坐标 × `TILE_SIZE`。
- 统一写两个工具函数 `gridToPixel(col, row)` 和 `pixelToGrid(x, y)`（放 `constants.ts` 或 `utils`），**禁止**散落各处手算 `col * TILE_SIZE`。

### 3.4 图块原点

所有网格物体（墙/地板/目标/箱子/玩家）统一 `setOrigin(0)`（左上角对齐）。否则 Phaser 默认以中心为原点，位置计算会错位。

### 3.5 命名与注释

- 场景类名用 `XxxScene`，key 用同名字符串 `'XxxScene'`。
- 逻辑步骤、坐标转换处写简短注释（中文即可），方便 review。

---

## 4. Phaser 4 关键 API 速查（已核实）

### 4.1 创建游戏

```ts
import Phaser from 'phaser';

new Phaser.Game({
  type: Phaser.AUTO,           // 自动选 WebGL / Canvas
  width: 800,
  height: 600,
  parent: containerElement,    // HTMLElement（React 的 ref.current）
  backgroundColor: '#1b1b1b',
  scene: [BootScene, GameScene],
});
```

### 4.2 定义场景

```ts
import Phaser from 'phaser';

export class GameScene extends Phaser.Scene {
  constructor() {
    super({ key: 'GameScene' });
  }

  // 生命周期：init(data) / preload() / create() 是可选的、被框架自动检测调用；
  // update(time, delta) 在类上正式声明。
  create(): void { /* 初始化、渲染 */ }
  update(time: number, delta: number): void { /* 每帧逻辑 */ }
}
```

### 4.3 图形（Shape，MVP 用这个，不加载图片）

```ts
// 画一个矩形（返回 Phaser.GameObjects.Rectangle）
const rect = this.add.rectangle(x, y, w, h, 0x888888, 1).setOrigin(0);
rect.setFillStyle(0x00ff00, 1);   // 改填充色
rect.setPosition(nx, ny);         // 移动
rect.setDepth(1);                  // 层级（数值越大越靠上）
```

> 同系列 Shape 还有 `this.add.circle / ellipse / line / polygon / triangle / star / grid`。玩家可用圆或矩形，MVP 建议都用矩形、用颜色区分。

### 4.4 文本

```ts
this.add.text(x, y, '步数: 0', {
  fontSize: '24px',
  color: '#ffffff',
  fontFamily: 'sans-serif',
});
```

### 4.5 键盘输入

```ts
// 方向键
const cursors = this.input.keyboard.createCursorKeys();
// cursors.left / right / up / down / space / shift，都是 Key 对象，有 .isDown / .isUp

// 单键（如 R 键重开）
const rKey = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.R);

// 单次触发（只按下那一下为 true，避免每帧重复触发）
if (Phaser.Input.Keyboard.JustDown(cursors.left)) { /* ... */ }
```

### 4.6 补间动画（移动用 tween 更顺滑）

```ts
this.tweens.add({
  targets: playerRect,   // 要移动的对象
  x: newPixelX,
  y: newPixelY,
  duration: 120,         // 毫秒
  onComplete: () => { /* 移动完成后的逻辑，如判定胜利 */ },
});
```

### 4.7 加载资源（MVP 可能用不到）

```ts
this.load.image('tiles', 'assets/tiles.png');
this.load.once(Phaser.Loader.Events.COMPLETE, () => {
  this.scene.start('GameScene');
});
```

### 4.8 场景切换

```ts
this.scene.start('GameScene');   // 停止当前、启动目标
this.scene.restart();            // 重开当前场景（R 键重开用这个）
```

---

## 5. 开发步骤

> 每步完成后：`npm run dev` 手动验证「验证点」，再 review 代码，最后 commit。

### 步骤 1：项目骨架 + Phaser 挂载

**目标**：让 React 页面里出现一块指定尺寸的游戏画布。

**要做的事**：

1. 建 `src/game/config.ts`：写一个 `createGame(parent: HTMLElement): Phaser.Game` 函数，返回 `new Phaser.Game({...})`，先不注册场景（或注册一个空场景占位）。
2. 改 `src/App.tsx`：
   - `const containerRef = useRef<HTMLDivElement>(null);`
   - `useEffect(() => { const game = createGame(containerRef.current!); return () => game.destroy(true); }, []);`
   - JSX 里渲染 `<div ref={containerRef} />`。
3. 清掉脚手架默认的 `App.css` 演示样式，保留一个能容纳画布的容器样式。

**关键 API**：`new Phaser.Game({...})`、`game.destroy(true)`。

**⚠️ React StrictMode 注意**：开发环境 React 19 会「挂载→卸载→再挂载」跑两次 effect。上面 `return () => game.destroy(true)` 的清理函数必须写，否则会创建两个游戏实例。如果暂时搞不定清理，先把 `main.tsx` 里的 `<StrictMode>` 去掉，后续再恢复。

**验证点**：页面显示一块 800×600 的画布，控制台无报错，React 刷新时不出现重复画布。

---

### 步骤 2：第一个场景 + 场景生命周期

**目标**：跑通「场景」这个概念——能创建场景、场景被启动、`create()` 被调用。

**要做的事**：

1. 建 `src/game/scenes/BootScene.ts`（可先做成极简，甚至 MVP 先省略）。
2. 建 `src/game/scenes/GameScene.ts`，`create()` 里用 `this.add.rectangle(...)` 画一个色块，再画一行 `this.add.text(...)` 提示文字。
3. 在 `config.ts` 的 `scene: [...]` 里注册场景。

**关键 API**：`super({ key: 'Xxx' })`、`create()`、`this.add.rectangle`、`this.add.text`。

**验证点**：画布上出现你画的色块和文字（说明场景真的启动了、`create` 真的跑了）。

---

### 步骤 3：关卡数据 + 解析函数（纯逻辑，无渲染）

**目标**：把关卡字符串变成三层运行时结构，先只在控制台验证。

**要做的事**：

1. 建 `src/game/types.ts`：定义 `enum Cell { Wall, Floor, Goal }`，以及 `LevelData`、`Vec2` 等类型。
2. 建 `src/game/data/levels.ts`：按 3.2 的格式写**第一关**（一个小关卡，含墙、地板、1~3 个目标点、1~3 个箱子、玩家）。
3. 建 `src/game/utils/level.ts`：写 `parseLevel(rows: string[]): LevelData`，实现 3.2 里描述的解析规则。
4. 在 `GameScene.create()` 里临时调用一次 `parseLevel(level1)`，把结果 `console.log` 出来。

**关键点**：
- 解析时校验：每行长度一致、玩家有且只有一个、目标点数量 == 箱子数量（否则无解）。
- 这一步**不碰任何渲染**，纯粹数据。

**验证点**：控制台打印的 `grid` / `boxes` / `player` 与关卡字符串一一对应，数量正确。

---

### 步骤 4：渲染地图

**目标**：把 `grid` 画出来，画面上呈现可辨认的关卡布局。

**要做的事**：

1. 在 `constants.ts` 定 `TILE_SIZE`（如 64），和每种格子的颜色。
2. 在 `GameScene` 里遍历 `grid`，用 `this.add.rectangle(...)` 画：
   - 墙：深色矩形。
   - 地板：浅色矩形。
   - 目标点：在地板上再叠一个小号的圆/矩形（不同颜色）。
3. 所有格子 `setOrigin(0)`，位置用 `gridToPixel` 换算。
4. 用 `setDepth` 控制层级：地板(0) < 目标标记(1) < 箱子(2) < 玩家(3)。

**关键 API**：`this.add.rectangle`、`setOrigin(0)`、`setDepth`、`gridToPixel`。

**验证点**：画面清晰显示出墙围成的区域、地板、目标点，与关卡字符串布局一致。

---

### 步骤 5：玩家移动 + 碰撞（不推箱子）

**目标**：方向键/WASD 移动玩家，撞墙不能通过。

**要做的事**：

1. 在 `GameScene` 里创建玩家矩形，记录 `player = {x, y}`。
2. 在 `update()` 里用 `createCursorKeys()` + `Phaser.Input.Keyboard.JustDown(...)` 监听方向键。
3. 写 `canMoveTo(col, row)` 辅助函数：越界或 `grid[y][x] === Cell.Wall` 则返回 false。
4. 玩家要移动：更新 `player` 坐标 + 用 tween 把矩形滑到新像素位置。

**关键 API**：`createCursorKeys()`、`JustDown()`、`this.tweens.add({...})`、`gridToPixel`。

**注意**：
- 用 `JustDown`（单次触发）而不是 `isDown`（持续触发），否则按住方向键会每帧走一格，太快。
- 移动过程中（tween 未结束）建议加个 `isMoving` 标志，防止连续输入导致坐标与画面脱节。

**验证点**：玩家能上下左右走，走到墙边停住，不穿墙；坐标与画面一致。

---

### 步骤 6：推箱子逻辑

**目标**：玩家能推箱子，箱子被墙/另一个箱子挡住时推不动。

**要做的事**：

1. 玩家朝某方向移动时，判断目标格：
   - 目标格是墙 → 玩家不动。
   - 目标格是箱子 → 再判断箱子**再往前进一格**的位置：
     - 是墙或另一个箱子 → 玩家和箱子都不动。
     - 否则 → 箱子也同向移一格，玩家前进一格（两者一起动）。
   - 目标格是空地/目标点 → 玩家前进一格。
2. 箱子用「矩形数组 + 位置数组」维护，和玩家一样坐标驱动、tween 平滑移动。

**关键点**：
- 判断「目标格是否被箱子占据」用 `boxes.some(b => b.x === cx && b.y === cy)`，封装成 `isBoxAt(col, row)`。
- 移动是「格子级」的：先算好所有受影响对象的新坐标，再统一 tween，不要边算边改。
- 目标点上的箱子（`*`）推离后，目标点仍留在 `grid` 里（这正是 3.1 分离的好处）。

**验证点**：能正常推箱子；箱子撞墙/撞箱子时玩家和箱子都不动，且画面不抖、坐标不漂移。

---

### 步骤 7：胜利判定 + 简单 UI + 重开

**目标**：推完所有箱子触发通关，显示步数，可重开。

**要做的事**：

1. 维护 `steps` 计数，每次玩家实际移动 +1，用文本显示「步数: N」。
2. 写 `checkWin()`：所有目标点格子上都有箱子（`grid` 里的每个 Goal 格都被某个 box 占据）→ 胜利。
3. 胜利后：显示「过关！」文字，并**停止输入**（`isMoving` 或一个 `won` 标志）。
4. 按 `R` 键调用 `this.scene.restart()` 重开。

**关键 API**：`this.add.text`、`this.scene.restart()`、`addKey(KeyCodes.R)`。

**验证点**：
- 所有箱子到位后立即显示过关提示，且此后方向键无效。
- 步数每次有效移动 +1，推箱子也算 1 步。
- 按 R 回到初始状态，步数归零。

---

## 6. MVP 完成定义（验收标准）

- [ ] 打开页面即可玩，无需额外操作。
- [ ] 方向键（可加 WASD）移动玩家，不能穿墙。
- [ ] 能推箱子，箱子不能推进墙或另一个箱子。
- [ ] 所有箱子到目标点后显示通关提示。
- [ ] 显示步数，按 R 键重开。
- [ ] 代码结构符合第 2 节目录，逻辑清晰、有注释，可 review。

---

## 7. 非 MVP（后续可扩展，本次不做）

- 多关卡 / 选关界面。
- 撤销（undo）、步数最优。
- 贴图、动画、音效。
- 箱子滑动的物理手感（惯性）。
- 触屏 / 虚拟方向键。

---

## 附：React 与 Phaser 的边界（重要）

- **Phaser 内部状态（步数、是否通关）不要放进 React state**，否则每帧/每次移动都触发 React 渲染，没必要。
- 如需 React 显示步数/按钮，用 Phaser 的 `game.events` 或自定义事件把数据**单向**抛给 React，React 只读不写。
- MVP 阶段建议：全部 UI 用 Phaser 的 `this.add.text` 直接画在画布上，最省事，不碰 React 通信。
